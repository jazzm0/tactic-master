<resources>

    <style name="AppTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/button_background_color</item>
        <item name="colorOnSurface">@android:color/white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="materialButtonStyle">@style/CustomSwitchMaterial</item>
    </style>

    <style name="TransparentDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="CustomSwitchMaterial" parent="Widget.Material3.CompoundButton.Switch">
        <item name="iconTint">@android:color/white</item>
        <item name="android:icon">@drawable/autoplay</item>
    </style>

</resources>